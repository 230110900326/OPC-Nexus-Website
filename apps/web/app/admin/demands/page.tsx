import type { Metadata } from "next";
import { DemandAdmin } from "../../../components/demand-admin";
export const metadata: Metadata = { title: "需求运营台 | OPC Nexus" };
export default function DemandAdminPage() { return <DemandAdmin />; }
