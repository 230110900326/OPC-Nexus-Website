import type { Metadata } from "next";
import { DemandAccount } from "../../../components/demand-account";
export const metadata: Metadata = { title: "我的需求 | OPC Nexus" };
export default function DemandAccountPage() { return <DemandAccount />; }
