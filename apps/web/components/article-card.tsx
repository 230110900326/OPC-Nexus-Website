import Link from "next/link";
import { Article } from "../lib/content";
const typeLabel = { news: "资讯", policy: "政策", insight: "深度" };
export function ArticleCard({ article }: { article: Article }) { const source = article.sources.find((item) => item.isPrimary) ?? article.sources[0]; return <article className="article-card"><Link className="article-card-image" href={`/articles/${article.slug}`} aria-label={article.title}>{article.coverImageUrl ? <img src={article.coverImageUrl} alt="" /> : <span>{typeLabel[article.type]}</span>}</Link><div className="article-card-body"><div className="article-meta"><span>{typeLabel[article.type]}</span><span>{article.category?.name ?? "未分类"}</span></div><h2><Link href={`/articles/${article.slug}`}>{article.title}</Link></h2><p>{article.summary}</p><footer><span>{source?.name ?? "OPC 编辑部"}</span><span>{article.publishedAt ? new Intl.DateTimeFormat("zh-CN", { month: "short", day: "numeric" }).format(new Date(article.publishedAt)) : "待发布"}</span><span>热度 {Number(article.heatScore).toFixed(0)}</span></footer></div></article>; }
