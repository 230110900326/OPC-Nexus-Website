import { calculateHeat, percentile } from "./heat-score";
const base = { likes: 10, comments: 4, favorites: 3, shares: 2, reads: 300, externalViews: 1_000, externalLikes: 100, ageHours: 1, keywordMatch: 0.5, sourceTrust: 0.8, editorScore: 0.5, contentType: "article" as const };
describe("Stage H - heat scoring", () => { it("keeps percentile values in a normalized range", () => { expect(percentile(10, [0, 10, 20])).toBeCloseTo(2 / 3); expect(percentile(1, [])).toBe(0); }); it("decays older content without producing a negative score", () => { const fresh = calculateHeat(base); const old = calculateHeat({ ...base, ageHours: 240 }); expect(fresh).toBeGreaterThan(old); expect(old).toBeGreaterThanOrEqual(0); }); it("uses a longer half-life for policy than community posts", () => { const policy = calculateHeat({ ...base, contentType: "policy", ageHours: 72 }); const post = calculateHeat({ ...base, contentType: "post", ageHours: 72 }); expect(policy).toBeGreaterThan(post); }); });
