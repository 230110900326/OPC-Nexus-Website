import { Column, CreateDateColumn, Entity, JoinColumn, ManyToOne, PrimaryGeneratedColumn, UpdateDateColumn, Unique } from "typeorm";
import { CreatorAccount, VideoPlatform } from "./creator-account.entity";
export enum SubtitleStatus { NOT_FETCHED = "not_fetched", AUTHORIZED = "authorized", PROCESSING = "processing", COMPLETED = "completed", FAILED = "failed", NO_PERMISSION = "no_permission" }
@Entity({ name: "videos" }) @Unique("UQ_videos_platform_id", ["platform", "platformVideoId"])
export class Video { @PrimaryGeneratedColumn("uuid") id!: string; @Column({ type: "varchar", length: 20 }) platform!: VideoPlatform; @Column({ name: "platform_video_id", length: 200 }) platformVideoId!: string; @Column({ length: 240 }) title!: string; @Column({ name: "cover_url", type: "varchar", length: 1000, nullable: true }) coverUrl!: string | null; @Column({ name: "original_url", length: 1000 }) originalUrl!: string; @ManyToOne(() => CreatorAccount, (account) => account.videos, { onDelete: "CASCADE" }) @JoinColumn({ name: "creator_account_id" }) creatorAccount!: CreatorAccount; @Column({ name: "published_at", type: "timestamptz", nullable: true }) publishedAt!: Date | null; @Column({ name: "duration_seconds", type: "integer", default: 0 }) durationSeconds!: number; @Column({ name: "platform_metrics", type: "jsonb", default: () => "'{}'::jsonb" }) platformMetrics!: Record<string, number>; @Column({ name: "platform_description", type: "text", default: "" }) platformDescription!: string; @Column({ name: "content_summary", type: "text", nullable: true }) contentSummary!: string | null; @Column({ name: "key_points", type: "jsonb", default: () => "'[]'::jsonb" }) keyPoints!: string[]; @Column({ name: "industry_tags", type: "jsonb", default: () => "'[]'::jsonb" }) industryTags!: string[]; @Column({ name: "chapters", type: "jsonb", default: () => "'[]'::jsonb" }) chapters!: { startSeconds: number; title: string }[]; @Column({ name: "subtitle_status", type: "varchar", length: 20, default: SubtitleStatus.NOT_FETCHED }) subtitleStatus!: SubtitleStatus; @Column({ name: "subtitle_source", type: "varchar", length: 240, nullable: true }) subtitleSource!: string | null; @Column({ name: "transcript", type: "text", nullable: true }) transcript!: string | null; @Column({ name: "summary_model_version", type: "varchar", length: 80, nullable: true }) summaryModelVersion!: string | null; @Column({ name: "is_published", default: true }) isPublished!: boolean; @CreateDateColumn({ name: "created_at", type: "timestamptz" }) createdAt!: Date; @UpdateDateColumn({ name: "updated_at", type: "timestamptz" }) updatedAt!: Date; }
