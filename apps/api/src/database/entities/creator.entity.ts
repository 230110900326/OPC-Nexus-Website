import { Column, CreateDateColumn, Entity, OneToMany, PrimaryGeneratedColumn, UpdateDateColumn } from "typeorm";
import { CreatorAccount } from "./creator-account.entity";
export enum AuthorizationStatus { PENDING = "pending", AUTHORIZED = "authorized", RESTRICTED = "restricted", REJECTED = "rejected" }
@Entity({ name: "creators" }) export class Creator { @PrimaryGeneratedColumn("uuid") id!: string; @Column({ length: 120 }) name!: string; @Column({ name: "avatar_url", type: "varchar", length: 500, nullable: true }) avatarUrl!: string | null; @Column({ type: "jsonb", default: () => "'[]'::jsonb" }) industries!: string[]; @Column({ name: "trust_level", type: "smallint", default: 3 }) trustLevel!: number; @Column({ name: "authorization_status", type: "varchar", length: 20, default: AuthorizationStatus.PENDING }) authorizationStatus!: AuthorizationStatus; @Column({ name: "is_enabled", default: false }) isEnabled!: boolean; @OneToMany(() => CreatorAccount, (account) => account.creator) accounts!: CreatorAccount[]; @CreateDateColumn({ name: "created_at", type: "timestamptz" }) createdAt!: Date; @UpdateDateColumn({ name: "updated_at", type: "timestamptz" }) updatedAt!: Date; }
