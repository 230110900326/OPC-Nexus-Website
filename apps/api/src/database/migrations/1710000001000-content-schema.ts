import { MigrationInterface, QueryRunner } from "typeorm";
export class ContentSchema1710000001000 implements MigrationInterface { name = "ContentSchema1710000001000";
  async up(q: QueryRunner) { await q.query(`CREATE TABLE categories (id uuid PRIMARY KEY DEFAULT gen_random_uuid(), slug varchar(80) UNIQUE NOT NULL, name varchar(80) NOT NULL, sort_order integer NOT NULL DEFAULT 0, is_active boolean NOT NULL DEFAULT true, parent_id uuid REFERENCES categories(id))`); await q.query(`CREATE TABLE tags (id uuid PRIMARY KEY DEFAULT gen_random_uuid(), slug varchar(80) UNIQUE NOT NULL, name varchar(80) NOT NULL)`); await q.query(`CREATE TABLE articles (id uuid PRIMARY KEY DEFAULT gen_random_uuid(), slug varchar(180) UNIQUE NOT NULL, title varchar(240) NOT NULL, summary varchar(800) NOT NULL, cover_image_url varchar(500), type varchar(20) NOT NULL, status varchar(20) NOT NULL DEFAULT 'draft', original_url varchar(1000) NOT NULL, published_at timestamptz, heat_score numeric NOT NULL DEFAULT 0, policy_issuer varchar(160), policy_number varchar(100), effective_date date, applicable_region varchar(100), policy_status varchar(40), policy_highlights text, impact_industries text, category_id uuid REFERENCES categories(id), operator_id uuid REFERENCES users(id))`); await q.query(`CREATE TABLE article_sources (id uuid PRIMARY KEY DEFAULT gen_random_uuid(), article_id uuid NOT NULL REFERENCES articles(id) ON DELETE CASCADE, name varchar(160) NOT NULL, url varchar(1000) NOT NULL, is_primary boolean NOT NULL DEFAULT true)`); await q.query(`CREATE TABLE content_tags (article_id uuid NOT NULL REFERENCES articles(id) ON DELETE CASCADE, tag_id uuid NOT NULL REFERENCES tags(id) ON DELETE CASCADE, PRIMARY KEY(article_id,tag_id))`); }
  async down(q: QueryRunner) { await q.query('DROP TABLE IF EXISTS content_tags'); await q.query('DROP TABLE IF EXISTS article_sources'); await q.query('DROP TABLE IF EXISTS articles'); await q.query('DROP TABLE IF EXISTS tags'); await q.query('DROP TABLE IF EXISTS categories'); }
}
