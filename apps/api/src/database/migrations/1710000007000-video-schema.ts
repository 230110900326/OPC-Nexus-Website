import { MigrationInterface, QueryRunner } from "typeorm";
export class VideoSchema1710000007000 implements MigrationInterface { name = "VideoSchema1710000007000"; async up(q: QueryRunner): Promise<void> { await q.query(`CREATE TABLE "creators" ("id" uuid PRIMARY KEY DEFAULT gen_random_uuid(), "name" varchar(120) NOT NULL, "avatar_url" varchar(500), "industries" jsonb NOT NULL DEFAULT '[]'::jsonb, "trust_level" smallint NOT NULL DEFAULT 3 CHECK ("trust_level" BETWEEN 1 AND 5), "authorization_status" varchar(20) NOT NULL DEFAULT 'pending' CHECK ("authorization_status" IN ('pending','authorized','restricted','rejected')), "is_enabled" boolean NOT NULL DEFAULT false, "created_at" timestamptz NOT NULL DEFAULT now(), "updated_at" timestamptz NOT NULL DEFAULT now())`); await q.query(`CREATE TABLE "creator_accounts" ("id" uuid PRIMARY KEY DEFAULT gen_random_uuid(), "creator_id" uuid NOT NULL REFERENCES "creators"("id") ON DELETE CASCADE, "platform" varchar(20) NOT NULL CHECK ("platform" IN ('bilibili','youtube','douyin')), "platform_account_id" varchar(160) NOT NULL, "display_name" varchar(160) NOT NULL, "profile_url" varchar(1000) NOT NULL, "is_enabled" boolean NOT NULL DEFAULT false, "created_at" timestamptz NOT NULL DEFAULT now(), "updated_at" timestamptz NOT NULL DEFAULT now(), CONSTRAINT "UQ_creator_accounts_platform_account" UNIQUE ("platform","platform_account_id"))`); await q.query(`CREATE TABLE "videos" ("id" uuid PRIMARY KEY DEFAULT gen_random_uuid(), "platform" varchar(20) NOT NULL CHECK ("platform" IN ('bilibili','youtube','douyin')), "platform_video_id" varchar(200) NOT NULL, "title" varchar(240) NOT NULL, "cover_url" varchar(1000), "original_url" varchar(1000) NOT NULL, "creator_account_id" uuid NOT NULL REFERENCES "creator_accounts"("id") ON DELETE CASCADE, "published_at" timestamptz, "duration_seconds" integer NOT NULL DEFAULT 0, "platform_metrics" jsonb NOT NULL DEFAULT '{}'::jsonb, "platform_description" text NOT NULL DEFAULT '', "content_summary" text, "key_points" jsonb NOT NULL DEFAULT '[]'::jsonb, "industry_tags" jsonb NOT NULL DEFAULT '[]'::jsonb, "chapters" jsonb NOT NULL DEFAULT '[]'::jsonb, "subtitle_status" varchar(20) NOT NULL DEFAULT 'not_fetched' CHECK ("subtitle_status" IN ('not_fetched','authorized','processing','completed','failed','no_permission')), "subtitle_source" varchar(240), "transcript" text, "summary_model_version" varchar(80), "is_published" boolean NOT NULL DEFAULT true, "created_at" timestamptz NOT NULL DEFAULT now(), "updated_at" timestamptz NOT NULL DEFAULT now(), CONSTRAINT "UQ_videos_platform_id" UNIQUE ("platform","platform_video_id"))`); await q.query(`CREATE INDEX "IDX_videos_feed" ON "videos" ("is_published","published_at" DESC)`); await q.query(`CREATE TABLE "video_sync_logs" ("id" uuid PRIMARY KEY DEFAULT gen_random_uuid(), "creator_account_id" uuid NOT NULL REFERENCES "creator_accounts"("id") ON DELETE CASCADE, "status" varchar(20) NOT NULL CHECK ("status" IN ('succeeded','failed')), "discovered_count" integer NOT NULL DEFAULT 0, "error_message" text, "created_at" timestamptz NOT NULL DEFAULT now())`); } async down(q: QueryRunner): Promise<void> { await q.query(`DROP TABLE IF EXISTS "video_sync_logs"`); await q.query(`DROP TABLE IF EXISTS "videos"`); await q.query(`DROP TABLE IF EXISTS "creator_accounts"`); await q.query(`DROP TABLE IF EXISTS "creators"`); } }
