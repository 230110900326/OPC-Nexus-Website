import { Repository } from "typeorm";
import { Notification, NotificationType } from "../database/entities/notification.entity";
import { NotificationsService } from "./notifications.service";
describe("Stage E - notifications", () => { const repository = { create: jest.fn((value) => value), save: jest.fn(async (value) => ({ id: "n1", ...value })), update: jest.fn(), findAndCount: jest.fn().mockResolvedValue([[], 0]), count: jest.fn().mockResolvedValue(0) } as unknown as Repository<Notification>; const service = new NotificationsService(repository); beforeEach(() => jest.clearAllMocks()); it("creates an event notification for the intended user and relies on the database unread default", async () => { await service.create("11111111-1111-4111-8111-111111111111", NotificationType.EVENT_STATUS_CHANGED, "活动更新", "报名已确认", "event", "22222222-2222-4222-8222-222222222222"); expect(repository.save).toHaveBeenCalledWith(expect.objectContaining({ type: NotificationType.EVENT_STATUS_CHANGED, targetType: "event" })); }); it("marks only the current user's notification as read", async () => { await service.markRead("11111111-1111-4111-8111-111111111111", "22222222-2222-4222-8222-222222222222"); expect(repository.update).toHaveBeenCalledWith({ id: "22222222-2222-4222-8222-222222222222", user: { id: "11111111-1111-4111-8111-111111111111" } }, { isRead: true }); }); });
