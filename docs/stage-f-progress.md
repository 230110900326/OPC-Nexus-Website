# 阶段 F 进度记录（资讯采集）

更新日期：2026-07-16

## 本次完成：Prompt 30—34

- 新增 `crawl_sources`、`crawl_jobs`、`crawl_logs` 实体、迁移 `1710000005000-crawl-sources` 与后台来源/任务/日志 API。
- 来源必须为 `authorized` 才可启用；默认创建的本地测试来源处于禁用状态。
- 优先关键词：`OPC 一人公司`、`OPC财经`、`OPC超级个体`；同时覆盖一人公司、超级个体、个体创业、个人品牌、灵活就业、小微企业、专精特新、数字经济、人工智能、普惠金融、减税降费、营商环境等。
- Python 爬虫接入 Redis 本地测试队列和可选定时任务；默认仅允许 `127.0.0.1` / `localhost`。
- 完成 HTML 文章解析、RSS/Atom/Sitemap 发现和固定样例政策适配器，均有测试覆盖。

## 验证

- API TypeScript 检查通过。
- API Jest：11 suites / 28 tests 通过。
- Python crawler：5 tests 通过。
- Web TypeScript 检查和生产构建通过。

## 后半段完成

- 文章去重按 canonical URL 和正文指纹执行；重复条目保留为附加来源，但不重复创建主内容。
- 配置化关键词可产生行业分类置信度；规则摘要保存版本、关键词、实体和人工审核标记。
- 新增审核队列、拒绝、合并与原文链接检查 API；审核前内容不会自动发布。

## 后续

1. 在可用 PostgreSQL/Redis 环境执行阶段 E/F 迁移，并验证 crawler 对本地测试页的完整入队、处理与日志写入。
2. 依据实际授权协议，将确认合法的财经和政策来源从 `pending` 更新为 `authorized`，再启用采集。
3. 继续阶段 F 后半段：去重、关键词分类、摘要、审核队列与失效检查。
