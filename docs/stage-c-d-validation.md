# 阶段 C–D 完整性检查

检查日期：2026-07-15

## 功能覆盖

| 阶段 | Prompt | 验收结果 |
|---|---:|---|
| C | 13 | 分类/标签父子层级、排序、启停、唯一约束与管理 API 已完成 |
| C | 14 | 文章、来源、政策字段、时间戳、约束与索引已完成 |
| C | 15 | 创建、编辑、预览、提交、退回、发布、计划发布、下线、恢复与操作者记录已完成 |
| C | 16 | 后台列表、状态/类型/来源/分类筛选、编辑、首图与发布时间已完成 |
| C | 17 | 推荐/最新/热度、行业分类、卡片字段与分页已完成 |
| C | 18–19 | 摘要详情、来源跳转、相关推荐、免责声明与政策详情已完成 |
| C | 20 | JPG/PNG/WebP 文件签名、5MB 限制、UUID 文件名、本地/S3 存储已完成 |
| C | 21 | 文章与社区帖 PostgreSQL 全文搜索、筛选和统一结果契约已完成；视频类型接口已预留，阶段 G 写入视频数据后接入结果源 |
| D | 22 | 板块、帖子、楼中楼评论、草稿/发布/隐藏、锁定/置顶/精华已完成 |
| D | 23 | 发帖、本人编辑删除、评论回复、分页、所有权与频率限制已完成 |
| D | 24 | 板块、列表、发帖、编辑、详情与递归评论页面已完成 |
| D | 25 | 文章/视频/帖子/评论点赞、内容收藏、用户/创作者关注及数据库去重已完成 |
| D | 26 | 举报、队列、目标预览、隐藏、封禁、帖子审核与操作日志已完成 |

## 自动化结果

- `npm run lint`：通过，API 与 Web 均执行严格 TypeScript 检查。
- `npm run test`：10 个测试套件、25 项测试全部通过。
- `npm run build`：NestJS 与 Next.js 生产构建通过，共生成 16 个应用页面。
- TypeORM 元数据：全部实体关系可解析，阶段 C–D 外键列名与迁移一致。
- 迁移契约：全文索引、内容约束、论坛、互动、举报和审核表均有自动化断言。
- `npm ci --ignore-scripts --dry-run --offline`：通过，锁文件可离线复现。
- `git diff --check`：通过，无空白错误。

## 环境限制与数据库实跑

检查机器未安装 Docker，且本机 `127.0.0.1:5432` 没有 PostgreSQL 服务，因此没有伪报“真实数据库迁移已执行”。当前已使用迁移契约测试和 TypeORM 元数据初始化覆盖结构错误；在具备 PostgreSQL 后还需执行：

```text
npm run build --workspace=@opc/api
npm run migration:run --workspace=@opc/api
npm run migration:show --workspace=@opc/api
```

然后使用编辑、运营、版主和普通用户四类测试账号完成一次浏览器冒烟测试。
